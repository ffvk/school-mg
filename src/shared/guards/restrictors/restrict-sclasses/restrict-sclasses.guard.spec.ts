import { RestrictSclassesGuard } from './restrict-sclasses.guard';

describe('RestrictSclassesGuard', () => {
  it('should be defined', () => {
    expect(new RestrictSclassesGuard()).toBeDefined();
  });
});
