import { RestrictHomeworksGuard } from './restrict-homeworks.guard';

describe('RestrictHomeworksGuard', () => {
  it('should be defined', () => {
    expect(new RestrictHomeworksGuard()).toBeDefined();
  });
});
